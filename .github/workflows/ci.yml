name: CI - Library API

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: library-api
    
    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd mongosh
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean compile
      env:
        MAVEN_OPTS: -Xmx1024m

    - name: Run unit tests
      run: mvn test
      env:
        SPRING_DATA_MONGODB_URI: mongodb://localhost:27017/library_test

    - name: Generate code coverage
      run: mvn jacoco:report

    - name: Build application
      run: mvn package -DskipTests

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4 # Atualizado para v4
      with:
        files: ./library-api/target/site/jacoco/jacoco.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

    - name: Upload build artifact
      uses: actions/upload-artifact@v4 # Atualizado para v4 (Corrige o erro de falha automática)
      with:
        name: library-api-jar
        path: library-api/target/library-api-*.jar

  sonarqube:
    needs: build-and-test
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    defaults:
      run:
        working-directory: library-api
    
    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Run tests and generate coverage
      run: mvn clean test jacoco:report
      env:
        SPRING_DATA_MONGODB_URI: mongodb://localhost:27017/library_test

    - name: SonarQube Scan
      if: ${{ env.SONAR_TOKEN != '' }}
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  integration-tests:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: library-api
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Run integration tests with Testcontainers
      run: mvn test -Pintegration
      env:
        MAVEN_OPTS: -Xmx1024m

  security-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: library-api
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Run OWASP Dependency Check
      run: mvn org.owasp:dependency-check-maven:check
      continue-on-error: true

  quality-gate:
    needs: [ build-and-test, sonarqube, integration-tests ]
    runs-on: ubuntu-latest
    if: always() && (needs.build-and-test.result == 'success')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download build artifact
      uses: actions/download-artifact@v4 # Atualizado para v4 para conversar com o upload
      with:
        name: library-api-jar
        path: library-api/target/

    - name: Check build success
      run: |
        if [ ! -f library-api/target/library-api-*.jar ]; then
          echo "Build artifact not found!"
          exit 1
        fi
        echo "✅ Build quality gate passed!"